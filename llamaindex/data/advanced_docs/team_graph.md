# 团队和系统关系示例

LinAI 团队负责本地知识库产品。

张明负责检索服务，主要维护 Retriever、rerank 和向量索引加载逻辑。

李雪负责文档解析服务，主要维护 PDF 解析、表格抽取和扫描件 OCR。

王磊负责 Agent 工作台，主要维护工具调用、工单创建和人工确认流程。

检索服务依赖文档解析服务提供的 Markdown 文本。

Agent 工作台依赖检索服务查询产品文档和故障复盘。

当 PDF 解析服务异常时，检索服务仍然可以查询旧索引，但新文档不会进入索引。

当检索服务异常时，Agent 工作台不能回答知识库问题，但仍然可以创建人工工单。
