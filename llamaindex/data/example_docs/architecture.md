# AI 应用架构笔记

一个可维护的 RAG 系统通常包含数据接入、数据处理、索引构建、检索、生成、评估和观测几个部分。

数据接入负责从文件、数据库、API 或业务系统读取原始资料。数据处理负责清洗文本、切分片段、补充元数据和生成向量。

索引层不应该直接绑定业务逻辑。业务代码应该通过检索器、查询引擎或智能体工具访问索引。

生产环境需要关注三类问题：检索是否命中正确上下文，生成是否忠实于上下文，系统出现异常时是否能追踪调用链路。
